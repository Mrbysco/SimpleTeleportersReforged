* Added a configurable teleport cooldown (default 3s, max 300s, 0 to disable) to prevent accidental immediate re-teleports
* Added "Teleport Cooldown: X Seconds" action bar message when stepping on a teleporter that is still cooling down
* Added redstonePreventsCooldown config (default true) so a redstone pulse can always trigger a teleporter regardless of any pending cooldown
